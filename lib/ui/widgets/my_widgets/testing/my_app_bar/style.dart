import 'package:flutter/material.dart';
// import 'package:projectmanager/util/styles/index.dart';

const Color appBarBackgroundColor = Colors.white;
const Color appBarBorderBottomColor = Color.fromARGB(15, 0, 0, 0);
const Color appBarIconColor = Colors.green;

const TextStyle appBarThemeTextStyle = TextStyle(
  // fontFamily: 'Montserrat',
  color: Colors.green,
  fontSize: 20,
  height: 0.5,
  fontWeight: FontWeight.w600,
);

// height of the app bar container
const double appBarContainerHeight = 80;

// height of the app bar content (text and leading icons)
const double toolbarHeight = 100;
