// Create this class, to pass initialValues and form input select options lists.
// For larger-scale or reusable widgets.
/*
class FormData {
  final Map<String, dynamic> initialValues;
  final Map<String, List<String>> options;

  FormData({
    required this.initialValues,
    required this.options,
  });
}

// Usage
final formData = FormData(
  initialValues: {'username': 'JohnDoe', 'email': 'john.doe@example.com'},
  options: {
    'userRoleIds': ['admin', 'user', 'guest'],
    'accountStatusIds': ['active', 'suspended', 'pending'],
  },
);
*/